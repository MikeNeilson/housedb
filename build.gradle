buildscript{
    dependencies {
       classpath 'org.postgresql:postgresql:42.2.18.jre7'
    }
}

plugins {
    id 'java'
    id "org.flywaydb.flyway" version "7.5.0"
}

flyway {
    url = DBURL
    user = DBUSER
    password = DBPASSWORD
    mixed = true
    schemas = ['housedb']
}
