#pragma once

#include "crow_all.h"

class LocationHandler {

    public:
        void routes(crow::SimpleApp &app);
        /*
        void get(const crow::request &req, crow::response &res);
        void get(const crow::request &req, crow::response &res, const std::string &name);
        void put(const crow::request &req, crow::response &res, const std::string &name);*/

};